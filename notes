//import React, {useState} from 'react';
  import '../../App.css';
  import ReactMapGL from "react-map-gl";


  function ParkingMapAmsterdam() {
      const [viewport, setViewport] = useState({
          latitude: 52.37403,
          longitude: 4.88969,
          width: "100vw",
          height: "100vh",
          zoom: 10
      });
      let mapboxgl = require('mapbox-gl/dist/mapbox-gl.js');

      mapboxgl.accessToken = 'pk.eyJ1Ijoia2FyaW5hcGllcGVyIiwiYSI6ImNrbzRncjk2aTBibjIydm15eHk5aWRmZG8ifQ.YvF0NbAYkRvqUn__AGHL9g';
      let map = new mapboxgl.Map({

          container: 'REACT_APP_MAPBOX_TOKEN',
          style: 'mapbox://styles/mapbox/streets-v11'
      });


  //console.log(mapb, "map");
  console.log(process.env.REACT_APP_MAPBOX_TOKEN, "token map!!!");
  //de console log en token heb ik even uitgezet.
      return (
          <>
              <link href='https://api.mapbox.com/mapbox-gl-js/v2.1.1/mapbox-gl.css' rel='stylesheet' />
              <div id="mapb">
                  <ReactMapGL {...viewport}
                       mapboxApiAccesToken={process.env.REACT_APP_MAPBOX_TOKEN}

                              onViewportChange={viewport => {
                                  setViewport(viewport);
                              }}
                  >
                      <div> locations on map</div>
                  </ReactMapGL>
              </div>
          </>
      );

  }
  export default ParkingMapAmsterdam;


